import React from 'react';
import styled from 'styled-components';

export const Header = () => {
	return <AppHeader />;
};

const AppHeader = styled.header`
	background: #524763;
	padding: 10px 5%;
`;
